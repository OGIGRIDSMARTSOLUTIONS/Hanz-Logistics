# Leave empty for local Vite development (including --host / LAN access)
# and for Vercel production (same-origin /api via vercel.json rewrites).
# Only set this if the API is hosted on a different origin.
VITE_API_BASE_URL=
VITE_QUOTE_ENDPOINT=
